name: Maven Build and Test

on:
  push:
    branches: [main, dev]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: 🔄 Checkout del código
        uses: actions/checkout@v3

      - name: 📁 Listar archivos raíz
        run: ls -la

      - name: 📁 Listar archivos Foro_hub
        run: ls -la ./Foro_hub

      - name: ⚙️ Configurar Java
        uses: actions/setup-java@v3
        with:
          distribution: 'temurin'
          java-version: '21'

      - name: 🛠️ Compilar y ejecutar tests con Maven
        run: mvn clean verify -B
        working-directory: ./Foro_hub

      - name: 📦 Mostrar versión de Maven
        run: mvn -version
